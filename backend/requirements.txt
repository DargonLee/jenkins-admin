# =============================================================================
# Jenkins Admin Backend - 简化依赖
# 只包含 Jenkins REST API 通信验证所需的核心依赖
# =============================================================================

# Web 框架
fastapi>=0.104.1,<0.120.0          # 现代 Python Web 框架
uvicorn[standard]>=0.24.0,<0.40.0  # ASGI 服务器

# 配置管理
pydantic>=2.5.0,<3.0.0             # 数据验证和设置管理
pydantic-settings>=2.0.0,<3.0.0    # 环境变量配置管理
python-dotenv>=1.0.0,<2.0.0        # .env 文件支持

# Jenkins 集成
python-jenkins>=1.8.0,<2.0.0       # Jenkins REST API 客户端

# 日志
structlog>=23.0.0,<26.0.0          # 结构化日志